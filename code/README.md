# subprocesswift

A description of this package.

```
swift build
swift run subprocesswift --help
swift run subprocesswift --path '/bin/ls' -a '-la' -a '/Users/Shared'   
swift build --configuration release
sudo codesign -s "Apple Development: Somewhere (E3PM1GCB4A)" -i subprocesswift ./.build/x86_64-apple-macosx/release/subprocesswift
codesign -dv --verbose=4 /usr/local/bin/subprocesswift
```
